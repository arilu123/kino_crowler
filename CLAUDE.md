Все принятые решения ты должен записывать себе в "WorkHistory.md" - это твоя память по проекту.

Цель данного проекта - составить дамп сайта https://kinopoisk.ru (или просто Сайт) в части информации о фильмах, актёрах и т.п.
Состав информации пока до конца не выявлен полностью, но на текущий момент точно известно, что нужно:
- базовая информация фильма (картины): год, жанты, режиссеры, сценаристы и т.п. - эта информация на "главной" странице фильма, например - https://www.kinopoisk.ru/film/78871/.

Ты должен складывать в файл "movie-info-list.md" список "атрибутов" фильма, который мы будем собирать. Выбери удобный для тебя формат. Но, учти, что эти атрибут нужно точно и аккуратно привязывать к расположению их на сайте. 


Конечная форма данных - Postgres (БД `kinopoisk`, локальный Postgres.app). Решение принято 2026-06-06: пишем сразу в БД, без промежуточного слоя json-файлов (подробности и схема - в WorkHistory.md и db/schema.sql). Папки MOVIES/STAFF упразднены.

Очередь будущих "закачек" теперь живёт в самой БД (флаги `persons.enriched=false`, `films.full_cast_fetched=false`) - отдельный файл-очередь не нужен. todo.md оставлен как человекочитаемый список задач проекта.

Процесс сбора я вижу в написании скрипта-js который будет собирать нужную информацию со страницы браузера и передавать, например (и это только пример) на локальную апи/llm или писать в файл (может ли js??), а другой процесс будет раскладывать уже в аккуратные файлы.

Нам доступны любые схемы и подходы, потому что этот будет происходить на этой же локальной машине, даже возможно в режиме разработки в VS Code.

Все итоговые json-файлы потом будут собраны и загружены в БД. А потом отдельно нужно будет следить за "новинками" и обновлять информацию. 

Смело задавай вопросы и уточнения.